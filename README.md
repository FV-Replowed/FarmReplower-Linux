# Farm Replower for Linux
Install FV Replowed natively on Linux.

## Running (Debian, amd64)
From a terminal, execute `chmod +x install_fvrp_deb.sh && sudo ./install_fvrp_deb.sh`.

To play, navigate to http://localhost in Basilisk, then create an account or use the test account.
